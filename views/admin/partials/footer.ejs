    <footer class="admin-footer">
        <p>&copy; <%= new Date().getFullYear() %> <%= app.name %>. <%= t('footer.rights') %></p>
    </footer>

    <style nonce="<%= cspNonce %>">
    .admin-footer {
        text-align: center;
        padding: 20px;
        color: var(--text-muted);
        font-size: 14px;
        margin-top: 40px;
        border-top: 1px solid var(--border);
    }
    </style>
</body>
</html>
